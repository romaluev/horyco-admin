version: '3.8'
services:
  app:
    container_name: ${API_CONTAINER_NAME}
    build:
      context: ./
      target: dev
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    ports:
      - "4003:3002"
